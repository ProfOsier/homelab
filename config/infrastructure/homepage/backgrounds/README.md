# Command Center Backgrounds

`noc-command-center.png` is the source artwork for the Homepage command-center backdrop. The deployable copy lives in `../images/noc-command-center.png`, because Homepage serves images from that directory.
